package macp;

import java.util.ArrayList;
import java.util.Collection;
import java.util.List;

import org.fxmisc.richtext.model.StyleSpansBuilder;

import com.sun.xml.internal.ws.policy.privateutil.PolicyUtils.Collections;

public class SolucaoLag {
	private void testLongText(){
	    /*List<Collection<String>> styles = new ArrayList<>(3);
	    styles.add(Collections.singleton("red"));
	    styles.add(Collections.singleton("green"));
	    styles.add(Collections.singleton("blue"));

	    String text = "This is a test\n";
	    int times = 3000;
	    StringBuilder sb = new StringBuilder();
	    StyleSpansBuilder<Collection<String>> ssb = new StyleSpansBuilder<>(times);
	    for (int i=0; i<times; i++) {
	        sb.append(text);
	        ssb.add(styles.get(i%3), text.length());
	    }

	    int oldLength = area.getLength();
	    area.appendText(sb.toString());
	    area.setStyleSpans(oldLength, ssb.create());

	    area.requestFocus();*/
	}
}
